version: '3.8'

services:
 api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/api
      - /api/node_modules
